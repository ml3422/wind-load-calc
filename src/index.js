import React from 'react';
import ReactDOM from 'react-dom/client';
import WindLoadModule from './WindLoadModule';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WindLoadModule />
  </React.StrictMode>
);